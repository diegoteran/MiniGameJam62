/// @description Insert description here
// You can write your code in this editor
var persistant_ghost;
with (other) {
	if (!ghost) {
		event_user(0);
	}
	persistant_ghost = ghost;
}
if (!persistant_ghost) {
	event_user(0);
}